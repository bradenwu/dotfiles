case ":$PATH:" in
    *":$HOME/bin:"*) ;;
    *) PATH="${PATH:+$PATH:}$HOME/bin" ;;
esac
case ":$PATH:" in
    *":$HOME/.local/bin:"*) ;;
    *) PATH="${PATH:+$PATH:}$HOME/.local/bin" ;;
esac
export PATH
